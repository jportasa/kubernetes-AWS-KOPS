apiVersion: v1
kind: Pod
metadata:
  name: nginx
spec:
#  nodeSelector:
#    disktype: ssd
  containers:
  - image: nginx:latest
    name: nginx
    volumeMounts:
    - mountPath: /test-ebs
      name: test-volume
  volumes:
  - name: test-volume
    # This AWS EBS volume must already exist.
    persistentVolumeClaim:
      claimName: myclaim